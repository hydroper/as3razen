use crate::ns::*;

pub(crate) struct DirectiveSubverifier;

impl DirectiveSubverifier {
    pub fn verify_directives(list: &[Rc<Directive>]) -> Result<(), DeferError> {
        todo_here()
    }
}